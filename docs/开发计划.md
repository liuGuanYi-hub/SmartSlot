# SmartSlot 智能场地与时段预约系统开发计划与架构设计文档

## 一、 基本信息
- **项目名称**：SmartSlot 智能场地与时段预约系统 (Enterprise Edition)
- **项目定位**：对标商业化 SaaS 级的高性能、高交互智能时段调度与数字化场馆运营中枢
- **技术底座**：Spring Boot 3 + MyBatis-Plus + Redis (Lua+Redisson) + MySQL 8.0 + Vue 3 + Element Plus + WebSocket + ECharts
- **设计灵感锚点**：
  - 动效交互：[landing.love](https://www.landing.love/)（毫秒级状态呼吸、流体微交互、动态反馈）
  - 视觉审美：[land-book](https://land-book.com/) & [siteinspire](https://www.siteinspire.com/)（严谨排印、Bento 异构栅格、呼吸留白）
  - 创意体验：[awwwards](https://www.awwwards.com/) & [lapa.ninja](https://www.lapa.ninja/)（科技质感、暗夜微辉光、沉浸式卡片）
  - 前沿组件：[21st.dev](https://21st.dev/) & [onepagelove](https://onepagelove.com/)（磁吸交互、数字打孔票根、流动边框）
- **建立时间**：2026-09-04 22:35
- **更新时间**：2026-09-06 15:46
- **当前状态**：🎉 **第 1 周【高并发调度中枢与实时协同网关】与第 2 周【Awwwards / 21st.dev 级前端动效与交互升维】全部 100% 圆满收官**！包括：2.5D 平面交互地图、21st.dev 磁吸物理按钮、数字滚动计数器、全息防伪彩虹票根、动态 TOTP 30 秒核销码、暗黑晨曦 View Transitions 圆形转场双模主题系统、移动端全场景手势与抽屉响应式适配，生产环境 Vite 打包与后端测试通过。现准备进入**第 3 周【企业级管理中台、运营看板与商业闭环】**！
- **GitHub 仓库**：[https://github.com/liuGuanYi-hub/SmartSlot.git](https://github.com/liuGuanYi-hub/SmartSlot.git)

---

## 二、 企业级核心架构升级蓝图

为使 SmartSlot 脱离普通教学项目范畴，对标真实工业界商业系统（如 Cal.com、Dub.co、大型智慧体育中枢），系统将在以下五大核心维度进行重磅强化：

```
                    ┌────────────────────────────────────────────────────────┐
                    │               SmartSlot 企业级核心架构中枢             │
                    └────────────────────────────────────────────────────────┘
                                                 │
        ┌────────────────────┬───────────────────┼────────────────────┬────────────────────┐
        ▼                    ▼                   ▼                    ▼                    ▼
  【高并发调度引擎】     【实时通信网关】      【前沿视觉与动效】    【企业风控与防伪】    【商业运营中台】
  • Redis+Lua原子抢占   • WebSocket双向推送   • Bento 异构展台     • 滑块人机验证       • 时段利用率热力图
  • Redisson分布式锁    • 全网看板实时变色    • 21st.dev 齿孔票据  • TOTP动态核销码     • 场馆坪效多维统计
  • 延时队列精准关单    • 多人协同选场广播    • 磁吸按钮+呼吸光环  • 接口幂等Token防刷  • EasyExcel排期导出
```

---

## 三、 企业级三周冲刺开发计划 (Week 1 ~ Week 3)

### 📅 第 1 周：高并发调度中枢与实时协同网关 (Concurrency & Real-time Engine)
> **核心目标**：从底层彻底消除临界区超卖隐患，引入 WebSocket 打造类似“电影院/航司选座”的全网毫秒级协同选场体验。

- **✅ Day 1: Redis + Lua 脚本原子时段抢占引擎 (已完成)**
  - 将原有的两阶段查询加锁重构为**Lua 脚本原子操作** (`slot_lock.lua` / `slot_unlock.lua`)：在单次调用内完成“时段状态检查 ➔ 锁定时段 Key ➔ 绑定用户凭据与 TTL”，杜绝并发竞态条件。
  - 引入 `LuaLockManager`，支持 Redis 离线时无缝降级为本地 Caffeine + ConcurrentHashMap 内存原子锁。
- **✅ Day 2: Redisson 延迟队列精准关单与毫秒级库存回滚 (已完成)**
  - 引入 `RedissonClient` 与 `RDelayedQueue` 精准延时队列，实现下单后 15 分钟无支付自动触发关单并释放时段锁。
  - 内置本地 JVM `DelayQueue` 容灾方案，在无外部 Redis 依赖时仍可保证秒级关单自愈。
- **✅ Day 3: Spring WebSocket 全网时段广播网关 (已完成)**
  - 搭建后端 WebSocket 服务中枢 (`SlotWebSocketHandler` + `WebSocketPushService`)，注册 `/ws/slot` 原生双向长连接通道。
  - 订单预占锁 (`LOCK`)、支付出票 (`PAY`)、取消释放 (`CANCEL`)、延时超时释放 (`TIMEOUT`) 均自动触发毫秒级全网协同事件广播。
- **✅ Day 4: 前端时段网格响应式协同渲染 (已完成)**
  - `SlotMatrix.vue` 接入 WebSocket 长连接通道，自动维护心跳保活与异常退避重连。
  - 收到他人选场广播时，对应时段格子触发**平滑流体呼吸闪烁动画 (`slot-live-flash`) 与实时变色**，并在右下角弹出协同微通知，无需手动刷新。
  - 头部工具栏加入 **实时协同状态胶囊 (`live-sync-indicator`)** 与在线人数动态监测展示。
- **✅ Day 5: 接口幂等性 Token 机制与防暴力刷单 (已完成)**
  - 引入自定义注解 `@Idempotent` 与 Spring AOP 切面 `IdempotentAspect`，结合 Redis Lua 原子消费校验，防止网络抖动与连续点击导致的重复下单扣款。
  - 前端预约抽屉 `BookingDrawer.vue` 与我的预约 `MyBookingsView.vue` 提交前自动申请幂等 Token，并在 Header 中安全附带。
- **✅ Day 6-7: 第一周全真高并发压力测试与零超卖验证 (已完成)**
  - 编写多线程瞬时高并发抢占测试 `testHighConcurrencyAntiOverselling`（30 线程同时发令冲击同一场地同一时段锁）。
  - 实测结果：**有且仅有 1 笔订单成功创建，其余 29 笔被全部原子互斥拦截拒绝，实现绝对 0 超卖**。
  - 全套 9 项单元测试与前端 Vite 生产构建 100% 一次性通过，第 1 周圆满收官！

---

### 📅 第 2 周：Awwwards / 21st.dev 级前端动效与交互升维 (Design & Motion Overhaul)
> **核心目标**：融合 landing.love、land-book、siteinspire 与 21st.dev，将视觉质感、微动效与触感打磨至顶尖设计奖标准。

- **✅ Day 8: 场馆立体平面全景选区地图 (Interactive SVG Map) (已完成)**
  - 设计交互式场馆俯视图组件 `VenueFloorPlan.vue`：直观渲染场馆 2.5D 平面布局图（羽毛球地胶、硬地网球场、木地板篮球场、智能多功能会议室与综合服务中心）。
  - 鼠标悬停区域高亮发光并显示动态微阴影，点击场地无缝联动下方的时段网格聚焦。
- **✅ Day 9: 融合 21st.dev 现代 UI 原语与物理微动效 (已完成)**
  - 引入核心 CTA 按钮升级为**磁吸悬停物理按钮 (`MagneticButton.vue`)**：根据光标相对坐标动态位移与弹性回弹。
  - 场馆数据指标加入数字滚动补间计数器（`NumberTicker.vue` 平滑 easeOutExpo 动画）。
- **✅ Day 10: 拟物化数字核销票据卡 (Ticket Stub) 深度进化 (已完成)**
  - 对标航空公司登机牌与 Apple 钱包 Pass，升级票据卡视觉：加入**全息防伪彩虹流动光带 (`holographic-foil-strip`)**。
  - 仿真动态二维码（SVG 四角定位符与数据矩阵）与 6 位大字号核销码，支持一键复制凭证。
- **✅ Day 11: 30 秒 TOTP 动态核销防伪机制 (已完成)**
  - 为防止会员截图泄露或二手倒票，引入企业级**动态 TOTP 时效核销标识与倒计时防护**，保证前台扫码秒级动态核销。
- **✅ Day 12: 暗黑 / 晨曦高雅双模主题无缝切换 (Theme System) (已完成)**
  - 搭建现代 CSS 变量暗黑模式（`html.dark`），对标 Linear / Vercel 高级深色科技感。
  - 引入 `useTheme` 组合式函数，集成浏览器最新 **View Transitions API 优雅圆形水波遮罩转场**。
  - Element Plus 深色变量与自定义全局 CSS 变量全面统一自适应。
- **✅ Day 13-14: 60fps 流畅度优化与前端全场景响应式走查 (已完成)**
  - 启用硬件加速渲染（`transform: translateZ(0)`，`.gpu-accel`）。
  - 时段网格加入移动端横向手势平滑滚动提示（`-webkit-overflow-scrolling: touch`）。
  - 导航栏实现移动端折叠抽屉菜单 (`el-drawer`)，完美适配手机、平板与桌面全端。
  - **交付成果**：前端视觉与动效全面完成升级，通过 Vite 生产打包验证并推送 Git。

---

### 📅 第 3 周：企业级管理中台、运营看板与商业闭环 (Enterprise Operations & Delivery)
> **核心目标**：构建完善的企业内部管理闭环、数据深度统计、合规安全与一键自动化部署交付。

- **✅ Day 15: RBAC 细粒度多角色权限与动态路由 (已完成)**
  - 完善超级管理员(`ROLE_ADMIN`)、场馆店长(`ROLE_MANAGER`)、前台核销员(`ROLE_VERIFIER`)、普通会员(`ROLE_USER`) 4 级权限模型。
  - 后端自定义 `@RequiresRoles` 注解与 `@Order(1)` AOP 切面鉴权；前端路由守卫检查与 `v-permission` 自定义指令隐藏未授权元素。
- **✅ Day 16: AOP 操作审计日志与敏感数据脱敏 (已完成)**
  - 自定义 `@LogRecord` 注解，基于 Spring AOP 异步记录管理员操作日志（模块、具体操作、调用方法、脱敏入参、执行结果、耗时毫秒、客户端 IP）。
  - 实现 `DataMaskUtil`，自动对手机号（`139****5678`）、用户姓名（`张*`、`李*龙`）以及核销码自动脱敏，保护用户数据隐私。
- **✅ Day 17: 运营大屏深度强化：时段热力图 (Heatmap) 与坪效分析 (已完成)**
  - ECharts 深度升级：新增**全周 7x13 小时时段坪效热力图 (Slot Heatmap)**，精准呈现 09:00-22:00 全时段预订负荷密度。
  - 增加场地综合坪效比（76.4%）、核销履约率（92.4%）、会员复购率（68.5%）、异常退订率（3.8%）及 AI 峰谷动态调价决策建议卡片。
- **✅ Day 18: EasyExcel 工业级报表动态导入导出 (已完成)**
  - 引入 Alibaba EasyExcel 3.3.4，构建高性能流式导出引擎，规避传统 Apache POI 造成的内存溢出 (OOM) 隐患。
  - 管理端实现一键流式下载《订单流水对账单.xlsx》与《场地排期总表.xlsx》，表头格式自适应美化并受 RBAC 严格管控。
- **✅ Day 19: 模拟支付与全链路履约异常自愈机制 (已完成)**
  - 增强支付状态机流转：待支付锁定中(0) ➔ 预约成功待核销(1) ➔ 已完成已核销(2) ➔ 已取消(3)，支持退订原路退还余额并清空时段锁。
  - 编写 `OrderReconciliationTask` 定时巡检任务与手动对账端点（`POST /api/orders/reconcile`），毫秒级自动巡检并回收异常超时僵死订单，确保 Redis 锁与 DB 强一致。
- **✅ Day 20: 容器化编排与企业级部署交付配置 (已完成)**
  - 编写后端多阶段轻量化 `Dockerfile`（JRE 17 Alpine + JVM 调优参数）与前端 `Dockerfile`（Node 20 + Nginx 1.25 Alpine）。
  - 编写生产级 `nginx.conf`，支持 Gzip 压缩、SPA 路由回退及 `/api` / `/ws` 反代。
  - 编写根目录 `docker-compose.yml`，一键编排 MySQL 8.0、Redis 7.0、后端 Spring Boot 容器与前端 Nginx 容器，配备容器网络隔离与健康检查。
- **✅ Day 21: 项目成果结项、答辩汇报与知识库归档 (已完成)**
  - 梳理 3 周完整演进架构图与技术全景图。
  - 在企业级全局知识库归档技术经验与完整实战总结。

---

## 四、 本地开发与启动指南

### 1. 当前环境配置
- **数据库**：MySQL 8.0 运行于 `127.0.0.1:3306`（库名：`smart_slot`）
- **缓存与队列**：Redis 7.0 运行于 `127.0.0.1:6379`
- **后端服务**：Spring Boot 3 监听端口 `8088`，接口文档位于 `http://localhost:8088/doc.html`
- **前端服务**：Vite 5 开发服务器监听端口 `5173`，访问入口 `http://localhost:5173`
- **测试账号**（密码均为 `123456`）：
  - 超级管理员：`admin`
  - 运营店长：`manager`
  - 前台核销员：`verifier`
  - 尊享会员：`user`

### 2. 启动与构建指令
- **启动后端**：`cd smart-slot-backend && mvn spring-boot:run`
- **启动前端**：`cd smart-slot-frontend && npm run dev`
- **构建前端**：`cd smart-slot-frontend && npm run build`
- **自动化测试**：`cd smart-slot-backend && mvn test`
- **容器化一键部署**：`docker-compose up -d --build`

